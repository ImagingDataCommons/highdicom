.. _introduction:

Introduction
============

The ``highdicom`` build distribution provides interfaces for creating derived DICOM objects, including Segmentation images and Structured Report documents.

The :mod:`highdicom` Python package contains several subpackages for different modalities and SOP classes.
